# Base URL of the .NET backend.
# The iOS simulator can reach localhost; the Android emulator needs 10.0.2.2,
# and a physical device needs your machine's LAN address.
EXPO_PUBLIC_API_URL=http://localhost:5187
